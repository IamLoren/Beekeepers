import styled from "styled-components";

export const StyledTreeWrapper = styled.div`
    height: 100%;
    display: flex;
    aling-items: flex-end;

    & img {
        object-fit: contain;
    }
`